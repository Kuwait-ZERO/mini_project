import 'package:flutter/material.dart';

class TipsPage {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("update a Pet"),
      ),
    );
  }
}
